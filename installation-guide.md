# 動画リストビュワー - 詳細インストール・実行ガイド

## 📋 必要な環境

### Windows環境
- Windows 10 または Windows 11
- 管理者権限（インストール時のみ）

## 🔧 事前準備（Node.jsのインストール）

### Step 1: Node.jsをダウンロード
1. ブラウザで https://nodejs.org/ja にアクセス
2. 「推奨版」（LTS版）をクリックしてダウンロード
3. ダウンロードされた `.msi` ファイルを実行

### Step 2: Node.jsをインストール
1. インストーラーを **管理者として実行**
2. 「Next」をクリックして進む
3. ライセンス条項に同意して「Next」
4. インストール先はデフォルトのまま「Next」
5. 「Automatically install the necessary tools」にチェックを入れて「Next」
6. 「Install」をクリック
7. インストール完了後「Finish」をクリック

### Step 3: インストール確認
1. **Windows + R** キーを押す
2. `cmd` と入力してEnterキー
3. コマンドプロンプトで以下を実行：
```
node --version
npm --version
```
4. バージョン番号が表示されればOK

## 📦 アプリケーションのセットアップ

### Step 4: ZIPファイルの解凍
1. ダウンロードした `video-viewer.zip` を右クリック
2. 「すべて展開」を選択
3. 展開先を選択（例：`C:\Users\[ユーザー名]\Desktop\`）
4. 「展開」をクリック

### Step 5: コマンドプロンプトでフォルダに移動
1. **Windows + R** キーを押す
2. `cmd` と入力してEnterキー
3. 以下のコマンドで解凍したフォルダに移動：
```
cd "C:\Users\[ユーザー名]\Desktop\video-viewer"
```
※ `[ユーザー名]` は実際のユーザー名に置き換えてください

### Step 6: 依存関係のインストール
コマンドプロンプトで以下を実行：
```
npm install
```
- 数分かかる場合があります
- 「added XXX packages」と表示されれば完了

## 🚀 アプリケーションの実行

### Step 7: アプリケーション起動
コマンドプロンプトで以下を実行：
```
npm start
```
- アプリケーションウィンドウが開きます
- 初回起動時は少し時間がかかる場合があります

## 📁 使用方法

### Step 8: 動画フォルダの準備
1. 動画ファイル（mp4, avi, mov, gif等）が入ったフォルダを用意
2. フォルダ内に動画ファイルが直接配置されていることを確認

### Step 9: フォルダ選択
1. アプリケーション上部の「フォルダを選択」ボタンをクリック
2. 動画が入ったフォルダを選択
3. 「フォルダーの選択」をクリック

### Step 10: 動画の確認
- 選択したフォルダ内の動画が自動的にループ再生で表示されます
- 各動画は個別にループ再生されます

## 🔄 便利な機能の使い方

### 履歴機能
1. 「履歴」ボタンをクリック
2. 過去に開いたフォルダが表示されます
3. フォルダ名をクリックで再度開けます

### ピン留め機能
1. フォルダを開いた状態で「ピン留め」ボタンをクリック
2. よく使うフォルダを固定できます
3. 履歴パネルの上部に表示されます

### ドラッグ&ドロップコピー
1. 動画をマウスでドラッグ開始
2. 青いオーバーレイが表示されます
3. ドロップするとコピー先フォルダ選択ダイアログが開きます
4. コピー先を選択してファイルをコピー

## ❌ トラブルシューティング

### アプリケーションが起動しない場合
1. Node.jsが正しくインストールされているか確認
2. コマンドプロンプトで `npm install` を再実行
3. ウイルス対策ソフトがブロックしていないか確認

### 動画が表示されない場合
1. フォルダに動画ファイルが含まれているか確認
2. サポートされている形式か確認（mp4, webm, mov, gif, avi, mkv, wmv）
3. ファイル名に特殊文字が含まれていないか確認

### 動画が再生されない場合
1. 動画ファイルが破損していないか確認
2. 他の動画プレイヤーで再生できるか確認
3. ファイルサイズが大きすぎないか確認

## 🔧 開発者向け情報

### 開発モードで起動
```
npm run dev
```
- 開発者ツールが自動で開きます
- デバッグ情報を確認できます

### アプリケーション終了
- ウィンドウの「×」ボタンをクリック
- または Alt + F4 キー

## 📞 サポート

問題が解決しない場合は、以下の情報をお知らせください：
- Windows のバージョン
- Node.js のバージョン
- エラーメッセージの内容
- 実行しようとした操作の詳細

